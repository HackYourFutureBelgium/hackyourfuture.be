import styled from "styled-components"

import { COLOR, SPACER } from "../../utils/constants"

export const StyledHeader = styled.header`
  display: flex;
  width: 100%;
  height: 65px;
  box-sizing: border-box;
  background-color: ${COLOR.CONCRETE};
  padding: ${SPACER.M};
`
