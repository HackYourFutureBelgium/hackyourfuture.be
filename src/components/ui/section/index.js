import {
  StyledSection as StyledSectionUI,
  StyledWrapper as StyledWrapperUI,
} from "./SectionContainer"

export const StyledSection = StyledSectionUI
export const StyledWrapper = StyledWrapperUI
