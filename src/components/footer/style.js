import styled from "styled-components"

import { SPACER, COLOR } from "../../utils/constants"

export const StyledFooter = styled.footer`
  width: 100%;
  padding: 50px ${SPACER.M};
  background-color: ${COLOR.CONCRETE};
  display: flex;
  flex-direction: column;
  align-items: center;
`
